{
  "name": "mochajs-pact-example",
  "version": "1.0.0",
  "description": "Mocha Pact example",
  "main": "index.js",
  "scripts": {
    "test": "rimraf pacts && mocha --recursive \"test/*.spec.js\" --timeout 10000",
    "publish": "node test/publish.js"
  },
  "license": "MIT",
  "devDependencies": {
    "@pact-foundation/pact": "^9.11.0",
    "axios": "^0.19.0",
    "chai": "^3.5.0",
    "mocha": "^5.1.1",
    "rimraf": "^2.6.2"
  }
}
